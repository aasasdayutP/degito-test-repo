# Solution Summary

ก่อนเริ่มแก้ ผมไล่ดู Data Flow ของระบบก่อนว่า Frontend, API และ Database เชื่อมกันยังไง เพื่อให้รู้ว่าปัญหาแต่ละจุดควรเริ่มดูจากตรงไหน และพยายามแก้เฉพาะส่วนที่จำเป็นโดยไม่เปลี่ยนโครงสร้างเดิมของโปรเจกต์มากเกินไป

บั๊กแรกคือหลังจากเปลี่ยน Project Status แล้วข้อมูลบนหน้าเว็บยังไม่เปลี่ยนทันที เพราะมีการแก้ค่าใน State เดิม ทำให้ React ไม่มองว่า State มีการเปลี่ยนแปลง ผมเลยปรับให้หลังจาก Update ผ่าน API สำเร็จแล้ว ดึงรายการ Project ใหม่อีกครั้งเพื่อให้ข้อมูลบนหน้าจอตรงกับ Database

บั๊กที่สองคือสามารถส่งข้อมูล Project ที่ไม่ครบเข้า API ได้ ผมเลยเพิ่ม Validation ทั้งฝั่ง Frontend และ Backend สำหรับ Project Name และ Client และเพิ่ม `trim()` เพื่อกันกรณีที่กรอกมาเป็นช่องว่างอย่างเดียว

บั๊กที่สามคือรายการ Project มีข้อมูลซ้ำ สาเหตุมาจากการ JOIN กับ `project_notes` ซึ่งเป็น One-to-Many ทำให้ Project เดียวสามารถออกมาได้หลายแถว ผมเลือกเอา JOIN ส่วนนี้ออกเพราะไม่ได้ใช้ข้อมูล Note ในหน้านี้ แทนที่จะใช้ `DISTINCT` เพื่อแค่ซ่อนข้อมูลที่ซ้ำ

บั๊กสุดท้ายคือเวลา API Error ฝั่ง Frontend ไม่ได้จัดการ Response ที่ไม่สำเร็จ ทำให้ผู้ใช้ไม่รู้ว่าเกิดอะไรขึ้น ผมเลยเพิ่มการตรวจสอบ Error และนำข้อความจาก Backend มาแสดงบนหน้าเว็บ

ส่วนฟีเจอร์ค้นหาด้วยชื่อลูกค้า ผมเลือกส่ง `clientName` ไปกรองที่ Backend แล้วใช้ `ILIKE` ใน PostgreSQL พร้อม Debounce ตอนพิมพ์ เพราะไม่ต้องโหลดข้อมูลทั้งหมดมา Filter ที่ Frontend และรองรับข้อมูลที่เยอะขึ้นได้ดีกว่า ข้อแลกเปลี่ยนคือมีการเรียก API เพิ่มและมี Delay เล็กน้อยจาก Debounce แต่ช่วยลด Request ที่เกิดถี่เกินไปได้

นอกจากนี้ผมปรับขนาด Column ในตารางให้ชัดเจนขึ้น เพื่อไม่ให้ Layout ขยับตามความยาวของค่า Status
